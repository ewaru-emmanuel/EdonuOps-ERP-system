# Pre-commit hook configuration
# Install: pip install pre-commit && pre-commit install

repos:
  - repo: local
    hooks:
      - id: check-tenant-queries
        name: Check Tenant Query Usage
        entry: python scripts/check_tenant_queries.py
        language: system
        types: [python]
        pass_filenames: true
        always_run: false

